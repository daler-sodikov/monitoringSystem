# Project conventions

## Package manager

Use **bun** for everything: `bun install`, `bun add`, `bun remove`, `bun run <script>`.
Do not use pnpm or npm in this project (user preference, applies to all of the user's apps). `bun.lock` is the lockfile.
